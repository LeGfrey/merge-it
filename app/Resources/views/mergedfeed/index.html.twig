{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        <i class="material-icons">merge_type</i>
        <span>My merged feeds</span>
    </h1>
    <div class="content">
        <a class="btn btn-success btn-raised" href="{{ path('user_merged_new') }}">
            <i class="material-icons">add_circle_outline</i>
            <span>Create a new entry</span>
        </a>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Name</th>
                <th class="actions-title">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for mergedFeed in mergedFeeds %}
                <tr>
                    <td><a href="{{ path('user_merged_show', { 'id': mergedFeed.id }) }}"><i class="material-icons">keyboard_arrow_right</i>{{ mergedFeed.name }}</a></td>
                    <td>
                        <ul class="actions">
                            <li>
                                <a href="{{ path('user_merged_show', { 'id': mergedFeed.id }) }}"><i class="material-icons">search</i></a>
                            </li>
                            <li>
                                <a href="{{ path('user_merged_edit', { 'id': mergedFeed.id }) }}"><i class="material-icons">create</i></a>
                            </li>
                            <li>
                                <a href="{{ path('user_merged_merge', { 'id': mergedFeed.id }) }}"><i class="material-icons">rss_feed</i></a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
